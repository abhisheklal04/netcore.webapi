version: '3'

services:
  ci-build:
    image: microsoft/dotnet:2.1.403-sdk
    volumes:
      - .:/src
    working_dir: /src
    command: /bin/bash -c "dotnet restore ./webapi.sln && dotnet publish ./webapi.sln -c Release -o ./obj/Docker/publish"
